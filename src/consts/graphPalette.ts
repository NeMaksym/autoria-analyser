import { blue, indigo, amber } from '@mui/material/colors';
import { alpha } from "@mui/material";

const GRAPH_PALETTE = {
    totalColor: alpha(blue[400], 0.15),
    mechanicColor: indigo[300],
    automatColor: amber[300],
}

export default GRAPH_PALETTE;
